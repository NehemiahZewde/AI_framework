# Archived Phase 2 Native File Chat

This folder preserves the working general chat application with OpenAI-native
file inputs before the local pandas tabular workspace was added.

It contains source files and pinned requirements only. It does not contain API
keys, user-uploaded files, caches, or temporary output.
