# Archived Phase 3B Conversational ML Setup

This directory preserves the working Phase 3B runner before the optional,
structured ML-preparation workflow was added. It includes source and dependency
files only; API keys, uploaded files, temporary output, and cache directories
are intentionally excluded.
