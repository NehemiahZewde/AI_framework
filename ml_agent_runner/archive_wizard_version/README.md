# Archived Guided Dataset Workflow

This folder preserves the earlier guided CSV dataset exploration workflow that
existed before the general conversational Phase 1 redesign.

It contains the wizard source and its pinned requirements, but does not contain
API keys, uploads, temporary files, generated outputs, caches, or `__pycache__`
directories. `README_wizard.md` preserves the earlier runner documentation.
